#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: a program to print
 * the alphabets using putchar function
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	char ch = 'a';

	while (c <= 'z')
	{
		putchar(ch);
		ch++;
	}
	putchar('\n');

	return (0);
}

